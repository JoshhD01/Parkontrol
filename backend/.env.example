# Supabase Postgres (preferred)
SUPABASE_DB_URL=postgresql://postgres:
# Oracle fallback (only if SUPABASE_DB_URL is not set)
DB_HOST=
DB_PORT=
DB_USERNAME=
DB_PASSWORD=
DB_SID=

# App
PORT=3000

# Auth
JWT_SECRET=change_this_secret

# DB schema sync (solo desarrollo)
DB_SYNC=false
